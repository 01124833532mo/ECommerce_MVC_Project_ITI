@model CategoryVm
@{
    ViewData["Title"] = "New Category";
}

<nav aria-label="breadcrumb" class="mb-4">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index" class="text-decoration-none">Categories</a></li>
        <li class="breadcrumb-item active">New Category</li>
    </ol>
</nav>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-transparent border-bottom">
                <h4 class="mb-0 fw-bold"><i class="bi bi-plus-circle me-2 text-primary"></i>New Category</h4>
            </div>
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    <partial name="_Form" model="Model" />
                    <hr class="my-4" />
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary rounded-pill px-4">
                            <i class="bi bi-check-circle me-2"></i>Save
                        </button>
                        <a class="btn btn-outline-secondary rounded-pill px-4" asp-action="Index">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
